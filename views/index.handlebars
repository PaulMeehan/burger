<div class="container">
    <div class="row">
        <div class="col-sm-12">
            <h1>Eat Da Burger!</h1>
        </div>
    </div>
    <div class="row">
        <div class="col-sm-6">
            <ul>
                {{#each burger}}
                    {{#unless devoured === true}}
                        {{>burgers/burger-block devoured=false}}
                    {{/if}}
                {{/each}}
            </ul>
        </div>
        <div class="col-sm-6">
            <ul>
                {{#each burger}}
                    {{#unless devoured === false}}
                        {{>burgers/burger-block devoured=true}}
                    {{/if}}
                {{/each}}
            </ul>        
        </div>
    </div>
    <div class="row">
        <div class="col-sm-4">
            {{!-- blank --}}
        </div>
        <div class="col-sm-4">
            <input type="text" id="newBurger"/>
            <button 
        </div>
        <div class="col-sm-4">
            {{!-- blank --}}
        </div>
    </div>
</div>